# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BeaconSender' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BeaconSender

  pod 'RxSwift', '~> 3.0'
  pod 'RxCocoa'

  target 'BeaconSenderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BeaconSenderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
